# =============================================================================
# Service — Native Estate frontend
# =============================================================================
apiVersion: v1
kind: Service
metadata:
  name: native-property
  namespace: native-property
  labels:
    app: native-property
    app.kubernetes.io/name: native-estate
spec:
  selector:
    app: native-property
  ports:
    - name: http
      port: 80
      targetPort: http
