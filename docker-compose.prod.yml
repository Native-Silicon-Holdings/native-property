# =============================================================================
# docker-compose.prod.yml — production override (Supabase-native)
# =============================================================================
# Pulls pre-built images from registry. Backend/Postgres are retired.
# =============================================================================
services:
  frontend:
    image: registry.internal:6800/native-property-frontend:latest
    restart: always
